# bundles

This folder contains bundled DevFlow assets. A fresh install keeps only the core skills:

- `bundles/skills/devflow/SKILL.md`
- `bundles/skills/devflow-init/SKILL.md`

Project-specific skills and rules should be imported locally through `devflow-init` or the `devflow` skill after installation. The panel is read-only for relationship viewing and validation.

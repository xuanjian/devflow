# 主题色配置规范

## 获取主题色

在 Taro 组件中通过 `@dhbfront-utils/shell_env` 获取主题色配置。

## 核心约束

- 动态主题色优先走运行时配置，而不是写死在样式里
- 主要按钮渐变、边框状态、徽章背景等都应优先从 `themeConfig` 取值
- 涉及动态颜色时，优先用内联 style 或辅助函数，不要依赖静态编译期常量

## 推荐字段

- `theme_color`
- `button_color_start`
- `button_color_end`
- `title_color`
- `message_color`

{
  "version": 1,
  "name": "ai-context entry",
  "purpose": "Stable AI entrypoint. Read JSON indexes first, then load source markdown/rules/skills only when selected by task context.",
  "workflowOrder": [
    "Read entry/profile/current JSON first.",
    "Use ai-my-pm as the workflow router for development, debugging, documentation, integration, research, and context-maintenance tasks.",
    "After ai-my-pm classifies the task, load only the selected project JSON, scene JSON, rules, skills, and superpowers.",
    "Superpowers are execution discipline selected by ai-my-pm; they are not the first routing layer."
  ],
  "defaultReadOrder": [
    "config/entry.json",
    "config/profile.json",
    "runtime/current.json",
    "runtime/tasks/<activeTaskId>.json",
    "config/projects/index.json",
    "matched config/projects/<project-id>.json",
    "config/scenes/index.json",
    "matched config/scenes/<scene-id>.json",
    "config/skills/skills.json only when skill selection is needed",
    "config/rules/rules.json only when rule lookup is needed"
  ],
  "routingDecision": {
    "projectsIndex": "Read config/projects/index.json first as the candidate list. Use id, name, technologyFamilyId, path, and summary to choose matched project JSON files.",
    "projectDetail": "After selecting a project, read config/projects/<project-id>.json for doc path, mounted skills, scenes, rules, and source read policy.",
    "scenesIndex": "Read config/scenes/index.json as the candidate list when the task may involve a workflow scene or multiple projects.",
    "sceneDetail": "After selecting a scene, read config/scenes/<scene-id>.json for included projects, mounted scene rules, composition reason, environment switching, run/debug, and source scene doc path.",
    "fallback": "If no project or scene is clear, use projects index and scenes index to ask one concise clarification instead of reading all source documents."
  },
  "sourceReadPolicy": {
    "projectDocs": "Read docs/repos/*.md only after a project JSON is selected and the summary is insufficient.",
    "scenes": "Read docs/scenes/*.md only after a scene JSON is selected and detailed workflow text is needed.",
    "rules": "Read config/rules/rules.json for applyMode, globs, and whenToRead. Active rule sources under bundles/rules/** are Markdown .md files and are loaded only when a selected project, selected scene, or task gate requires full rule details. Project JSON triggers project-on-demand rules. Scene JSON triggers scene-on-demand rules. Read docs/reference/rules/** only when an active rule points there and compact guidance is insufficient.",
    "skills": "Read active bundles/skills/** only when ai-my-pm, a selected project JSON, or an explicit user request triggers the skill. Read docs/reference/skills/** only for archived detail when the active skill is insufficient. Superpowers are selected after ai-my-pm classification, not before it.",
    "profile": "Read docs/person/profile.md only when long-term preferences or project map details matter."
  },
  "installation": {
    "script": "scripts/install-ai-context.mjs",
    "commands": [
      "install",
      "install --project-skills",
      "check",
      "uninstall",
      "sync-projects",
      "sync-projects --write",
      "sync-projects --project <project-id> --write",
      "sync-projects --project <project-id> --skills-only --write",
      "validate"
    ],
    "skillLinks": [
      "~/.agents/skills/ai-context -> <ai-context-root>/bundles/skills/ai-context",
      "~/.codex/skills/ai-context -> <ai-context-root>/bundles/skills/ai-context",
      "~/.claude/skills/ai-context -> <ai-context-root>/bundles/skills/ai-context"
    ],
    "globalEntrypoints": [],
    "globalEntrypointsStatus": "deprecated; do not create home-level AGENTS.md, CLAUDE.md, or WORK_CONTEXT.md by default",
    "projectEntrypoints": [
      "AGENTS.md",
      "CLAUDE.md",
      ".ai-configs/claude.md",
      ".claude/CLAUDE.md",
      ".cursor/rules/00-ai-context.mdc"
    ],
    "legacyProjectEntrypointsToPrune": [
      "claude.md"
    ]
  },
  "taskFlow": {
    "currentPointer": "runtime/current.json",
    "activeTaskPattern": "runtime/tasks/<task-id>.json",
    "gatesPath": "config/tasks/gates.json",
    "gates": [
      "G1",
      "G2",
      "G3",
      "G4",
      "G5",
      "G6",
      "G7"
    ],
    "currentWorkMarkdown": "deprecated; do not read or update runtime/current-work.md"
  },
  "indexes": {
    "profile": "config/profile.json",
    "projects": "config/projects/index.json",
    "scenes": "config/scenes/index.json",
    "skills": "config/skills/skills.json",
    "rules": "config/rules/rules.json"
  }
}

---
description: 测试规范：单元测试和UI测试
alwaysApply: false
---

# 测试规范

## 单元测试

### 测试目录
`DHBTests/`

### 命名规范
`TestClassName.m`

### 测试覆盖范围
- 核心业务逻辑
- 数据模型解析
- 工具类方法
- Service 层业务方法

### 单元测试示例

```objc
#import <XCTest/XCTest.h>
#import "GoodsBaseDTO.h"

@interface TestGoodsBaseDTO : XCTestCase
@end

@implementation TestGoodsBaseDTO

- (void)testJSONParsing {
    NSDictionary *json = @{
        @"goods_id": @"123",
        @"goods_name": @"测试商品"
    };
    
    GoodsBaseDTO *goods = [GoodsBaseDTO yy_modelWithJSON:json];
    
    XCTAssertNotNil(goods);
    XCTAssertEqualObjects(goods.goodsId, @"123");
    XCTAssertEqualObjects(goods.goodsName, @"测试商品");
}

- (void)testArrayParsing {
    NSArray *jsonArray = @[
        @{@"goods_id": @"123", @"goods_name": @"商品1"},
        @{@"goods_id": @"456", @"goods_name": @"商品2"}
    ];
    
    NSArray *goods = [NSArray yy_modelArrayWithClass:[GoodsBaseDTO class] json:jsonArray];
    
    XCTAssertEqual(goods.count, 2);
}

@end
```

## UI测试

### 测试目录
`DHBUITests/`

### 测试覆盖范围
- 关键业务流程
- 用户操作路径
- 页面跳转流程

### UI测试示例

```objc
#import <XCTest/XCTest.h>

@interface DHBUITests : XCTestCase
@end

@implementation DHBUITests

- (void)setUp {
    [super setUp];
    self.continueAfterFailure = NO;
    [[[XCUIApplication alloc] init] launch];
}

- (void)testLoginFlow {
    XCUIApplication *app = [[XCUIApplication alloc] init];
    
    // 输入用户名
    XCUIElement *usernameField = app.textFields[@"用户名"];
    [usernameField tap];
    [usernameField typeText:@"test_user"];
    
    // 输入密码
    XCUIElement *passwordField = app.secureTextFields[@"密码"];
    [passwordField tap];
    [passwordField typeText:@"test_password"];
    
    // 点击登录按钮
    [app.buttons[@"登录"] tap];
    
    // 验证登录成功
    XCTAssertTrue([app.navigationBars[@"首页"] exists]);
}

@end
```

## 测试建议

### 1. 单元测试
- 测试独立的功能模块
- 模拟(Mock)外部依赖
- 测试边界条件和异常情况
- 保持测试简单、快速

### 2. UI测试
- 测试完整的用户流程
- 使用有意义的 accessibility 标识
- 避免依赖具体的UI文本（支持多语言）
- 考虑不同设备和屏幕尺寸

### 3. 测试覆盖率
- 核心业务逻辑：>80%
- 工具类方法：>90%
- UI交互：关键流程全覆盖

### 4. 持续集成
- 每次提交代码前运行测试
- CI/CD 流程中自动运行测试
- 测试失败时不允许合并代码

# docs

Fresh install keeps this folder intentionally minimal. Project docs, scene docs, references, and handoff notes should be created locally by `ai-context-init` or by panel actions after the user chooses what to register.
